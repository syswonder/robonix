# Set torque limit per joint (0-1).  NOT exposed as MCP — same reasoning as
# set_speed: motor parameters belong in skill/service task context.
[contract]
id      = "robonix/primitive/hand/set_torque"
version = "1"
kind    = "primitive"
idl     = "hand/srv/SetTorque.srv"

[mode]
type = "rpc"
