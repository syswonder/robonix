# Continuous ROS 2 topic stream of current joint positions.
# Publishes JointState messages (float64[] positions) at driver-native rate.
[contract]
id      = "robonix/primitive/hand/state_joint"
version = "1"
kind    = "primitive"
idl     = "hand/msg/JointState.msg"

[mode]
type = "topic_out"
