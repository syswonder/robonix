# Speaker audio stream input (raw PCM or encoded).
[contract]
id      = "robonix/primitive/audio/speaker"
version = "1"
kind    = "primitive"
idl     = "audio/msg/AudioChunk.msg"
description = "Audio playback stream consumed by an audio output primitive."

[mode]
type = "topic_in"
