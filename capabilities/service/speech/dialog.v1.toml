# Voice dialog session manager.
# Orchestrates mic → ASR → intent → TTS → speaker pipeline.
# Manages VAD, dialog state machine, and language switching.
[contract]
id      = "robonix/service/speech/dialog"
version = "1"
kind    = "service"
idl     = "speech/srv/StartDialog.srv"
description = "Run a voice dialog session that coordinates speech input and output."

[mode]
type = "rpc_server_stream"
