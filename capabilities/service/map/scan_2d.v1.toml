# 2D LaserScan projected from 3D registered point cloud.
# Consumable by Nav2 costmap obstacle layer.
[contract]
id      = "robonix/service/map/scan_2d"
version = "1"
kind    = "service"

[io.msg]
msg = "sensor_msgs/msg/LaserScan"

[mode]
type = "topic_out"

