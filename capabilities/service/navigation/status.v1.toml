# Query the status of a previously issued navigation goal.
# ROS IDL: `lib/service/navigation/srv/GetNavigationStatus.srv` (goal_id → known + status + terminal).
[contract]
id      = "robonix/service/navigation/status"
version = "1"
kind    = "service"
idl     = "service/navigation/srv/GetNavigationStatus.srv"

[mode]
type = "rpc"
