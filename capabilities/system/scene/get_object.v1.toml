# Direct lookup by stable object_id. Returns full record incl.
# first_seen / last_seen / observation_count.
[contract]
id      = "robonix/system/scene/get_object"
version = "1"
kind    = "system"

[io.srv]
srv = "semantic_map/srv/GetObject"

[mode]
type = "rpc"
