# Geometry layer: scoped surfaces + reference to Nav2's 2D occupancy
# grid topic. No voxel data shipped through MCP in v1.
[contract]
id      = "robonix/system/scene/get_semantic_map"
version = "1"
kind    = "service"

[io.srv]
srv = "semantic_map/srv/GetSemanticMapSlice"

[mode]
type = "rpc"
