# One edge of the query star graph: `relation` must hold between the target
# and whatever `anchor` names.
# Relation must be one Scene maintains: near, on_top_of, under, inside,
# contains, reachable_by.
string relation
# A class name, an operator label, or an object id.
string anchor
