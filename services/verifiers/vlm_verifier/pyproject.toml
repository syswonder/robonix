[project]
name = "robonix-vlm-verifier"
version = "0.1.0"
description = "Generic single-image visual result verification"
requires-python = ">=3.10"
dependencies = ["robonix-api", "grpcio-tools", "httpx", "Pillow>=10"]

[tool.uv]
package = false

[tool.uv.sources]
robonix-api = { workspace = true }
