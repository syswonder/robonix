# Exercise the mapping (map) service save_map RPC against the live SLAM database.
name: mapping_save
task: "ci-test: save the current slam map under a test id"
steps:
  - content: Saving the current map.
    rtdl_description: map save
    status: in_progress
    caps:
      - match: map_save_map
        args:
          map_id: ci_test_map
          note: robonix ci
        description: checkpoint the slam map
  - content: Map saved.
    status: done
expect_contracts:
  - map/save_map
expect_args:
  - ci_test_map
