
1. 
```
// system/executor/src/main.rs
robonix_scribe::info("executor", "robonix-executor starting");
log::info!
```
why two types of log info function? we should remove the previous log crate and use scribe crate as the info! warn! etc. macros

为什么有两种类型的日志信息功能？我们应该移除之前的日志crate，使用scribe crate来作为info!、warn!等宏

2. 

```
// system/liaison/examples/mock_audio.rs
#[tokio::main]
async fn main() -> Result<()> {
    env_logger::Builder::from_env(env_logger::Env::default().default_filter_or("info")).init();
    robonix_scribe::info("mock_audio", "mock audio starting");
```
robonix_scribe：：info 太长了，请像以前一样把它们做成宏

3. 

```
system/liaison/src/main.rs
        .unwrap_or_else(|| "robonix_liaison=info".to_string());
    env_logger::Builder::from_env(env_logger::Env::default().default_filter_or(log_filter)).init();
    let _ = args.log.clone().or_else(|| std::env::var("RUST_LOG").ok());
    robonix_scribe::info("liaison", "robonix-liaison starting");
```

currently, the tag field should be the capability provider id if the process is a capability provider. However, let the programmer write "liaison" each time using the info function is definitely not good. We should provide some init function that can set all logs from this process with prefilled tags as its capability provider id string or whatever, thus we won't need to write the tag each time
目前，如果这个进程是能力提供者，标签字段应该是能力提供者的 ID。不过，每次用 info 函数让程序员写“liaison”显然不太好。我们应该提供一些初始化函数，可以将这个进程的所有日志的标签预先填充为它的能力提供者 ID 字符串或其他东西，这样就不用每次都写标签了。
