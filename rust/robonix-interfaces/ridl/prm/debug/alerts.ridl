namespace robonix/prm/debug

import std_msgs/msg/String

stream alerts
    @desc("Alert messages stream. Provider subscribes (receives), consumer publishes (sends).")
{
    input msg String @desc("Alert message from consumer to provider");
    version 1.0;
}
