namespace robonix/prm/arm

import sensor_msgs/msg/JointState

stream state_joint
    @desc("Arm joint state stream. Use for LLM/agent to read current joint angles.")
{
    output joint_states JointState @desc("name[], position[], velocity[], effort[]");
    version 1.0;
}
