manifestVersion: 1

build: bash scripts/build.sh

start: |
  CF=(-f compose.yaml)
  if command -v nvidia-smi &>/dev/null && nvidia-smi &>/dev/null; then
    CF+=(-f compose.gpu.yaml)
    echo "[tiago_sim_stack] NVIDIA GPU detected — merging compose.gpu.yaml"
  fi
  exec docker compose "${CF[@]}" up --build ros2-bridge

package:
  name: com.robonix.example.tiago_sim_stack
  version: 0.1.0
  vendor: robonix
  description: Tiago sim stack — Docker Compose with Webots, Nav2, and tiago_bridge
  license: MulanPSL-2.0

capabilities:
  - name: robonix/prm/camera/rgb

depends: []
