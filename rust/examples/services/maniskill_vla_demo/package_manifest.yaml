manifestVersion: 1

build: bash scripts/build.sh

# Main node: env (ManiSkill3 environment)
start: exec uv run python3 -m maniskill_vla_demo.env_node

# Other nodes (to be split into separate packages later):
#   perception: exec uv run python3 -m maniskill_vla_demo.perception_node
#   grasp:      exec uv run python3 -m maniskill_vla_demo.grasp_node
#   viz:        exec uv run python3 -m maniskill_vla_demo.viz_node
#   mapping:    exec bash docker/run_rtabmap.sh

package:
  name: com.robonix.example.maniskill_graspnet_demo
  version: 0.2.0
  vendor: robonix
  description: ManiSkill3 GraspNet demo — env, perception, GraspNet manipulation
  license: MulanPSL-2.0

capabilities: []

depends: []
