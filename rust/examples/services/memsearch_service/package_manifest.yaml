manifestVersion: 1

build: bash scripts/build.sh

start: |
  export PYTHONPATH="$(pwd)/proto_gen:${PYTHONPATH:-}"
  exec python -m memsearch_service.service

package:
  name: com.robonix.example.memsearch_service
  version: 0.1.0
  vendor: robonix
  description: Memsearch MCP Server for Agent memory
  license: MulanPSL-2.0

capabilities:
  - name: robonix/srv/memory/search

depends: []
