// SPDX-License-Identifier: MulanPSL-2.0
// robonix-liaison library entry — exposes generated proto types so examples
// (mock_pilot, mock_audio, voice_demo_client) can reuse the same wire types
// without each emitting their own copy of the codegen output.

pub mod pb;
