# ridlc tests

- **`test_codegen.sh`** — runs `ridlc`, checks generated workspace layout and Python syntax.

From `rust/crates/ridlc`:

```bash
./tests/test_codegen.sh
```

Requires ROS 2, `rclpy`, and `grpcio` where applicable.
