# robonix-codegen tests

- **`test_codegen.sh`** — runs `robonix-codegen`, checks generated workspace layout and Python syntax.

From `rust/crates/robonix-codegen`:

```bash
./tests/test_codegen.sh
```

Requires ROS 2, `rclpy`, and `grpcio` where applicable.
