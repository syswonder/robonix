# LLM-facing capability description. Pilot builds one of these per
# (cap_id, contract_id) pair it wants to expose to the model.
string cap_id
string contract_id
string description
string input_schema_json
