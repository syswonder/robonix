# `lib/prm_*` — primitive (prm) ROS packages

Each **`prm_<domain>`** directory is a ROS 2 package name (see `lib/<package>/msg|srv`). The control-plane namespace is **`robonix/prm/<domain>/...`**. See the mapping tables in `rust/robonix-interfaces/README.md`.
