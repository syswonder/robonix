# Execute a natural-language or structured manipulation instruction.
# The VLA / scripted policy interprets the instruction and drives the arm.
# Input:  std_msgs/String.data — instruction text or JSON-encoded action spec.
# Output: std_msgs/String.data — execution result / status JSON.
[contract]
id      = "robonix/prm/manipulation/exec"
version = "1"
kind    = "primitive"

[io]
input  = "std_msgs/msg/String"
output = "std_msgs/msg/String"

[mode]
type = "rpc"

[semantics]
preemptable = true
