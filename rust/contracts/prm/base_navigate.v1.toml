# Send a navigation goal to the robot base.
# ROS IDL: `lib/prm_base/srv/Navigate.srv` (geometry_msgs/PoseStamped goal → accepted + status_message).
[contract]
id      = "robonix/prm/base/navigate"
version = "1"
kind    = "primitive"


[io.srv]
srv = "prm_base/srv/Navigate"

[mode]
type = "rpc"

[semantics]
preemptable = true
