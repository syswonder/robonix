# Persist a fact / user preference to long-term memory.
# ROS IDL: `lib/memory/srv/Save.srv`.
[contract]
id      = "robonix/system/memory/save"
version = "1"
kind    = "system"


[io.srv]
srv = "memory/srv/Save"

[mode]
type = "rpc"

[semantics]
persistent = true
