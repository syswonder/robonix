# ROS IDL: `lib/pilot/srv/HandleIntent.srv`. Stream element type is **only** in this TOML.
[contract]
id      = "robonix/srv/runtime/pilot"
version = "1"
kind    = "service"

[io.srv]
srv = "pilot/srv/HandleIntent"

[mode]
type = "rpc_server_stream"

[semantics]
stateful      = true
interruptible = true
