# Same as Pilot. ROS: `lib/liaison/srv/SubmitTask.srv`.
[contract]
id      = "robonix/srv/liaison"
version = "1"
kind    = "service"

[io.srv]
srv = "liaison/srv/SubmitTask"

[mode]
type = "rpc_server_stream"

[semantics]
unified_entry = true
interruptible = true
