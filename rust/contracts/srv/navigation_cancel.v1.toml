# Cancel an active navigation goal.
# ROS IDL: `lib/prm_base/srv/CancelNavigation.srv` (goal_id → accepted + message).
[contract]
id      = "robonix/srv/navigation/cancel"
version = "1"
kind    = "service"


[io.srv]
srv = "prm_base/srv/CancelNavigation"

[mode]
type = "rpc"
