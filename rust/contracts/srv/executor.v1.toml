# ROS: `lib/executor/srv/Execute.srv`.
[contract]
id      = "robonix/srv/executor"
version = "1"
kind    = "service"

[io.srv]
srv = "executor/srv/Execute"

[mode]
type = "rpc_server_stream"

[semantics]
stateless = true
