# ROS: `lib/executor/srv/ListTools.srv`.
[contract]
id      = "robonix/srv/runtime/executor/list_tools"
version = "1"
kind    = "service"

[io.srv]
srv = "executor/srv/ListTools"

[mode]
type = "rpc"

[semantics]
stateless = true
