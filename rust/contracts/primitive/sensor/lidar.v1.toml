# Provider streams `sensor_msgs/LaserScan`; gRPC `Stream(Empty) returns (stream LaserScan)`.
[contract]
id      = "robonix/primitive/sensor/lidar"
version = "1"
kind    = "primitive"


[io.msg]
msg = "sensor_msgs/msg/LaserScan"

[mode]
type = "topic_out"

[semantics]
realtime = true
