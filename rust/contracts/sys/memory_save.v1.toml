# Persist a fact / user preference to long-term memory.
# ROS IDL: `lib/memory/srv/Save.srv`.
[contract]
id      = "robonix/sys/memory/save"
version = "1"
kind    = "service"


[io.srv]
srv = "memory/srv/Save"

[mode]
type = "rpc"

[semantics]
persistent = true
