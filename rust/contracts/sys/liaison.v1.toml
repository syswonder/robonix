# Same as Pilot. ROS: `lib/liaison/srv/HandleIntent.srv`.
[contract]
id      = "robonix/sys/runtime/liaison"
version = "1"
kind    = "service"

[io.srv]
srv = "liaison/srv/HandleIntent"

[mode]
type = "rpc_server_stream"

[semantics]
unified_entry = true
interruptible = true
