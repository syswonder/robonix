# ROS: `lib/vlm/srv/ChatStream.srv`. Stream element type in TOML only.
[contract]
id      = "robonix/sys/model/vlm/chat"
version = "1"
kind    = "service"

[io.srv]
srv = "vlm/srv/ChatStream"

[mode]
type = "rpc_server_stream"

[semantics]
multimodal = true
tool_use   = true
