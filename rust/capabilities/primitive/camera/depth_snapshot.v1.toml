# On-demand depth image snapshot (single frame).
# Complements robonix/prm/camera/depth (continuous stream).
# ROS IDL: `lib/primitive/camera/srv/GetCameraImage.srv` (same wire as RGB snapshot; encoding differs).
[contract]
id      = "robonix/primitive/camera/depth_snapshot"
version = "1"
kind    = "primitive"


[io.srv]
srv = "camera/srv/GetCameraImage"

[mode]
type = "rpc"

