# Microphone audio stream (raw PCM or encoded).
# ROS: `audio/msg/AudioChunk` published on a topic.
[contract]
id      = "robonix/primitive/audio/mic"
version = "1"
kind    = "primitive"

[io.msg]
msg = "audio/msg/AudioChunk"

[mode]
type = "topic_out"

