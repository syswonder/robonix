# Compact and summarise recent memory entries.
# ROS IDL: `lib/memory/srv/Compact.srv` — unary RPC, empty request, `std_msgs/String` summary.
[contract]
id      = "robonix/system/memory/compact"
version = "1"
kind    = "system"


[io.srv]
srv = "memory/srv/Compact"

[mode]
type = "rpc"
