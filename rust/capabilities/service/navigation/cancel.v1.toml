# Cancel an active navigation goal.
# ROS IDL: `lib/service/navigation/srv/CancelNavigation.srv` (goal_id → accepted + message).
[contract]
id      = "robonix/service/navigation/cancel"
version = "1"
kind    = "service"


[io.srv]
srv = "navigation/srv/CancelNavigation"

[mode]
type = "rpc"
