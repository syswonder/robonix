# Switch SLAM operating mode (mapping / localization / idle).
[contract]
id      = "robonix/service/slam/switch_mode"
version = "1"
kind    = "service"

[io.srv]
srv = "slam/srv/SwitchMode"

[mode]
type = "rpc"

