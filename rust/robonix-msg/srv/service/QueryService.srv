# QueryService service (robonix spec)
string name                    # Standard service name
string filter                  # JSON string: filter by metadata (e.g., {"model":"deepseek"}). Empty string means no filter
---
robonix_core/ServiceInstance[] instances

