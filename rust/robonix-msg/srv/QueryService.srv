# QueryService service (EAIOS spec)
string name                    # Standard service name
string filter                  # JSON: filter by metadata (e.g., {"model":"deepseek"})
---
ServiceInstance[] instances

ServiceInstance:
    string provider
    string entry
    string metadata

