syntax = "proto3";
package filexfer;

service FileSink {
  rpc Upload(FileUpload) returns (Ack);
}

message FileUpload {
  string filename = 1;  
  bytes  data     = 2;  
}

message Ack {
  string status = 1;    
}

